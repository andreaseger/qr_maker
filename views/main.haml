-title "QR Generator"

-if @qr
  %p
    %strong
      Message:
    =@message
  %table
    - @qr.modules.each_index do |x|
      %tr
        - @qr.modules.each_index do |y|
          - if @qr.dark?(x,y)
            %td.black
          -else
            %td.white
